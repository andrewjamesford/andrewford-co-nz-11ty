---
layout: layout.liquid
---



{%- for article in collections.articles reversed -%}
    <em class="article-date">{{ article.date | date: "%Y-%m-%d" }}</em>
    <h2><a href="{{ article.url }}">{{ article.data.title }}</a></h2>
{%- endfor -%}

