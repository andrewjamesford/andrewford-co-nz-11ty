<!doctype html>
<html lang="en">
    <head>
        <meta charset="utf-8">
        <title>{{ title }}</title>
        <link rel="stylesheet" href="{{ '/css/normalize.css' | url }}">
        <link rel="stylesheet" href="{{ '/css/style.css' | url }}">
    </head>
    <body>
        <header>
        
        </header>
        <main>
            <h1>{{ title }}</h1>

            {{ content }}
        </main>
    </body>
</html>